export class Employee {
   

    constructor(  //esto es lo mismo que definir las variables como globales con el this.variable por ejemplo.
        public id: string,
         public name: string,
          public lastname: string, 
          public role: string) {}

    }